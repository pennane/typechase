const config = {
    ports: {
        server: 80,
        gateway: 8002,
        app: 8080
    },
    publicPorts: {
        server: 80,
        gateway: 8002,
        app: 80
    }
}

export default config
