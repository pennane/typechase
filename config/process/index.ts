const config = {
    ports: {
        server: 3003,
        gateway: 3002,
        app: 8080
    },
    publicPorts: {
        server: 8003,
        gateway: 8002,
        app: 80
    }
}

export default config
